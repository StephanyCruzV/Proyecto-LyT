
function test_operations ( ) is
    a : int := 5;
    b : int := 2;
    c : int := 3;
begin
    a := a - b + c ;
    b := (a + c * c) / 2.0;
    c := 1 * (2 + 3) + 4;

    print a;
    print b;
    print c;
end test_operations;
