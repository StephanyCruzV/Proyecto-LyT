function factorial(n : int) return int is
        result : int := 1;
    begin
        for i in 1..3 loop
            result := expression;
        end loop;

        return expression;
    end factorial;