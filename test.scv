
function factorial(n : int) return int is
    result : int := 1;
    result : float := 0.0;
begin
        for i in 1..(n + 1) loop
            result := result * i;
        end loop;

        return result;
end factorial;
