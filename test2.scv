
function test_operations ( ) is
    a : int := 7;
    b : float := 4;
    c : int := 2;
begin
    a := a - b + c ;
    b := (a + c * c) / 2.0;
    c := 1 * (2 + 3) + 4;

    print b;
end test_operations;
