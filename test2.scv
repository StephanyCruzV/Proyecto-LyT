
#| function test_operations ( ) is
    a : int := 1;
    b : float := 0.0;
    c : int := 5;
    result : float := 0;
begin
        a := a + a ;
        b := c * a 
        if b > c then
            result := a + 1;
        else
            result := a - 1;
        end if ;

        return result;
end test_operations;
|#

function test_operations ( ) is
    a : int := 1;
    b : float := 0;
    c : int := 5;
begin
    a := a + a ;
    b := c * a;

    return b;
end test_operations;

